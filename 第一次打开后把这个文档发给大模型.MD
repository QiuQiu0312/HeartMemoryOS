# 第一次打开后把这个文档发给大模型

> 把整个 HeartMemoryOS 项目根目录和本文件一起交给能够读取、修改并运行项目代码的编程大模型。本文件是接入与部署操作手册；不要只发送单个代码片段，也不要让普通聊天模型在无法读取项目时凭空猜部署方案。

## 1. 你接手的项目

这是“心忆 MemoryOS V2”，一套面向长期陪伴型虚拟恋人的可迁移记忆基础设施。当前交付同时包含：

- 已经可以运行和测试的 Node.js + SQLite 便携参考实现；
- 可直接打开的单用户、单伴侣本地聊天体验页，以及可编辑的安全默认人设；
- HTTP API、JavaScript SDK、响应式记忆控制台和版本化 Prompt；
- OpenAPI、JSON Schema 与 PostgreSQL/RLS/Outbox 生产参考契约；
- 正式 APP/Web 所需的架构、安全、迁移、成本与验收规则。

必须先区分两种状态：

1. **便携参考实现已经完成并在声明范围内验证**，可本机体验、单实例验证和作为接入起点。
2. **目标产品的正式生产部署尚未自动完成**，仍需结合它的身份系统、数据库、关系图、Worker、Push、云环境和合规要求实施。

不要把“目录里有 PostgreSQL SQL、Dockerfile 或生产架构”说成“已经存在可直接承载海量用户的公网集群”。

## 2. 三类权威说明与阅读顺序

项目只有三类主说明。源码仓库中它们都在根目录；便携发行包为了让小白一眼看见入口，会把用户信和本文件放在外层。用户信与 README 属于同一类，不是两套互相冲突的产品规范：

1. 用户说明：外层 `给用户的一封信.MD` 是快速入口；项目内 `README.md` 是完整项目介绍、本机体验和当前边界。
2. `第一次打开后把这个文档发给大模型.MD`：你正在阅读的实施手册。
3. `技术架构与部署说明.md`：领域模型、数据流、安全、成本、部署和验收的完整技术参考。

执行任务时按“状态与代码优先”的顺序阅读：

1. `README.md`、`package.json`、`.env.example`、`技术架构与部署说明.md`。
2. 根据任务读取实际源码和测试：
   - `packages/memory-core/src/` 与 `packages/memory-core/test/`；
   - `packages/runtime/src/` 与 `packages/runtime/test/`；
   - `apps/api/src/` 与 `apps/api/test/`；
   - `packages/client-js/src/` 与测试；
   - `apps/console/app/` 与控制台测试。
3. 读取 `contracts/openapi.yaml`、相关 JSON Schema 和 `contracts/postgresql-production.sql`。
4. 只有准备接入具体模型能力时，再读取 `prompts/registry.seed.json` 和对应 YAML Prompt。
5. 如果用户提供目标 APP、网站或机器人项目，继续读取目标项目的入口、依赖、鉴权、关系表、消息存储、模型调用、部署、测试和项目级开发说明。

文档不能覆盖代码事实；代码存在也不能自动证明生产就绪。遇到冲突时，以实际源码、机器契约和本次实际测试结果为依据，并把冲突报告给用户。

## 3. 阅读后的第一条回复

你在行动前必须确认自己已经理解：

- 你理解这是同一套内核的便携实现与生产适配方案，不是两个互不兼容的产品；
- 你理解便携版能运行，但正式生产仍需针对目标项目适配；
- 你理解 Claim、Evidence、Revision、Correction、Consent、Tombstone 和 Suppression；
- 你理解 Realm、Attribution、双时间和关系作用域；
- 你不会让模型决定身份、权限、游标、删除、授权或是否发送消息；
- 你不会编造没有执行过的测试、迁移和部署结果。

这些是你的内部实施要求，不要把 Claim、Tombstone 等术语清单原样念给普通用户。第一条回复先用四句话以内说清：

> 我已经读完项目和部署要求。这套内核既能在本机验货，也能接进现有 APP、网页或机器人；正式商用会根据你的服务器和用户规模做生产适配。如果你已经有成品或半成品，我会先只读检查并做好备份，不会要求你推倒重做。你不需要懂数据库或代码，只要从下面选一个最接近的情况。

不要只问“你打算用在哪里”这种开放问题。多数用户不知道该怎样描述技术需求。第一轮直接给出下面的生活化选项，并告诉用户回复最接近的编号即可：

> 先确认你目前最接近哪一种情况，不懂技术也没关系，可以只回复编号：
>
> 1. 只想在自己的电脑上测试或给自己使用；
> 2. 想放到自己的服务器或 VPS 上，主要仍是自己使用；
> 3. 已经有成品或半成品 APP、网页聊天或机器人，只想替换或升级原来的记忆架构；
> 4. 还没有项目，想从零做一个个人使用的虚拟伴侣；
> 5. 还没有项目，准备做可以给多位用户使用或商业运营的产品；
> 6. 目前只想先了解和验货，还没决定。
>
> 如果拿不准，回复“6”并用一句话说你现在手里有什么，我会替你判断。选择 3 时，请提供完整项目目录；只有截图或几段代码不足以安全接入。

这只是第一轮分流，不是让用户一次回答所有技术问题。收到答案后，先复述你的理解，再进入下一轮需求卡。

## 4. 用选项引导用户完成需求确认

不要用专业名词盘问普通用户，也不要让用户先选择数据库、向量库、队列或模型。能从目标项目和服务器读到的内容直接检查，不重复提问。

### 4.1 第二轮简短需求卡

根据第一轮答案删掉明显无关的问题，把仍然未知的选项发给用户。允许用户用“1B、2C、3A”这种方式回答，也允许用日常语言回答。

> 1. 最终运行在哪里？
>    - A. 只在自己的电脑；
>    - B. 自己的服务器、VPS 或 NAS；
>    - C. 已有公司的云环境；
>    - D. 不知道，请你检查后推荐。
>
> 2. 谁会使用？
>    - A. 只有我一个人；
>    - B. 少量内部测试人员；
>    - C. 多用户公开产品；
>    - D. 以后可能商用，但现在先做原型。
>
> 3. 每位用户会有几个虚拟伴侣？
>    - A. 固定一个；
>    - B. 以后可能有多个，但彼此记忆隔离；
>    - C. 多个伴侣，用户可以选择共享某几条记忆；
>    - D. 还没决定。
>
> 4. 现在有没有项目？
>    - A. 没有；
>    - B. 有半成品；
>    - C. 有已经能使用的成品，只替换记忆部分；
>    - D. 有项目，但我不清楚完成度。
>
> 5. 现有项目包含什么？
>    - A. APP；
>    - B. 网页；
>    - C. 机器人或第三方聊天渠道；
>    - D. 同时包含多种；
>    - E. 没有或不清楚。
>
> 6. 现在是否已经有后端或管理后台？
>    - A. 都没有；
>    - B. 有产品后端，但没有管理页面；
>    - C. 有后端，也有运营或管理后台；
>    - D. 不清楚，请你检查项目。
>
> 7. 原来的聊天和记忆怎么办？
>    - A. 不需要旧数据；
>    - B. 必须保留旧聊天，但旧记忆可以重新整理；
>    - C. 旧聊天和旧记忆都要迁移；
>    - D. 不清楚，请先盘点并给方案。

如果用户选择个人使用，不再追问日活和商业容量；如果用户选择多用户或商用，再继续询问规模、预算、数据地区和合规。不要把“暂时不知道”当成阻塞，先通过只读检查给出推荐默认值与理由。

### 4.2 这套架构能适配哪些组合

同一套领域内核可以覆盖这些情况，但“可以适配”不等于“完全不用改代码”：

| 用户情况 | 推荐落地方式 |
| --- | --- |
| 本机、单用户、单伴侣 | 便携 SQLite 实现即可完成验货和有限单实例使用 |
| 私人服务器、单用户 | 可使用加固后的单实例服务；即使只有一人，公网暴露仍需要正式鉴权、HTTPS、备份和非演示配置 |
| 多用户或商业产品 | 使用 PostgreSQL、强身份隔离、独立 Worker/调度器、备份监控和生产验收 |
| 一个用户一个伴侣 | 固定 relationship/companion 作用域 |
| 一个用户多个伴侣 | 架构已有 companion 与 relationship 隔离；默认互不共享 |
| 多伴侣选择性共享 | 只共享用户明确选择的单条记忆，并绑定目标关系授权 |
| 已有成品或半成品 | 保留原项目身份、消息与界面，在聊天回合边界接入 Memory API |
| 全新项目 | 先完成显式记忆闭环，再逐层加入摘要、Embedding、主动消息等能力 |
| 已有管理后台 | 优先复用原有登录、权限和设计系统；心忆控制台是可选参考界面，不是强制替换 |

单用户与多用户、单伴侣与多伴侣使用相同领域模型；差别主要在存储、身份、容量、管理界面和基础设施适配。

### 4.3 产品与规模

- 注册用户、日活、峰值同时聊天人数、每天消息量；
- 一个用户有几个虚拟恋人；
- 记忆是否允许跨虚拟恋人共享；
- 是云端、本地还是混合存储；
- 预计费用和数据保留期限；
- 个人使用、内部测试、公开免费服务还是商业运营；
- 数据主要位于哪个国家或地区。

### 4.4 当前技术

- 后端语言、框架和部署方式；
- 登录、Token、多租户与用户—关系—虚拟恋人数据结构；
- 消息的唯一权威数据库和 message ID；
- 数据库、队列、对象存储、KMS、监控和备份；
- 主聊天模型、Embedding、Push 或机器人渠道。

### 4.5 已有项目还要确认

- 项目是半成品还是已经有真实用户；
- 只替换记忆模块，还是允许调整聊天回合和数据结构；
- 旧记忆目前存在哪里、格式是什么、是否必须保留；
- 现有 API 返回格式能否保持兼容，以减少 APP 或网页改动；
- 是否允许短暂停机，还是必须灰度迁移；
- 原后端、原管理后台和原聊天数据库分别由谁负责。

### 4.6 产品规则

- 哪些类别可以自动成为候选，哪些必须逐条确认；
- 原始聊天不保存、滚动保留还是保留到删除；
- 是否允许语义索引和外部 Embedding；
- 是否开放用户触发的深度查找；
- 是否需要单次提醒、周期提醒、首次欢迎或关系主动关心；
- 用户如何查看、纠正、删除、禁记和导出。

不要要求非技术用户先选择 pgvector、Redis 或某个模型。根据规模给出默认建议、原因、费用影响和未来迁移路径。

## 5. 路线 A：本机体验

本机交付包含两条互不混淆的体验路线：

1. **零模型费用验货**：运行环境检查、自动化测试和隔离行为演示，验证记忆、召回、纠正、删除与 Token 裁剪；不需要填写任何模型 API。
2. **真实聊天体验**：打开本地聊天页，用户在右上角设置中配置自己的主聊天模型 API，实际走完“准备上下文—模型回复—提交消息—后台整理”的链路。调用主模型或后台整理模型会产生对应 Provider 的 Token 费用。

本地聊天页是为了让没有现成 APP 或网站的人能够测试、临时自用和观察记忆，不是已经完成公网安全、支付、账号体系、Push 和海量并发的商业聊天前端。它不能替代目标 APP 的生产验收。

如果用户不懂代码，优先让用户双击启动文件，不要先丢一串命令。只有启动失败、用户要求技术验货或你正在修改代码时，才逐项运行命令并用一句人话解释每一步证明了什么。

### 环境

- Node.js 22.13+，推荐 Node.js 24 LTS；
- 默认不需要 Docker；
- 零费用验货不需要任何模型 API；真实聊天至少需要一个 OpenAI-compatible 主模型 API；
- 后台整理模型不填写时自动复用主模型；Embedding 独立可选，绝不能把聊天模型自动当作 Embedding 模型；
- 本地体验不需要 Push API。

最简单的启动方法：

- macOS：双击 `启动心忆.command`；
- Windows 10/11：双击 `启动心忆-Windows.bat`。如果缺少 Node.js，它会先征求同意，再尝试通过 winget 安装，或者打开 Node.js 官方下载页；
- 当前发布前的完整实机验证以 macOS 为准，Windows 启动逻辑已做跨平台实现与自动化检查，但没有在真实 Windows 10 机器上完成最终实机保证，必须如实告诉用户。

第一次运行会安装本地界面依赖并生成只保存在这台电脑的配置。浏览器自动打开后：

请主动向小白用户解释安装体积：便携 ZIP 排除了可重装且与操作系统相关的 `node_modules`，所以下载包很小；首次启动必须联网安装界面依赖，当前版本在 Mac 上实测可能增加约 700～800 MB，建议至少预留 1 GB。不得把依赖下载误说成模型下载、聊天数据或 Token 费用。若用户要求完全离线分发，应按目标操作系统另做并测试平台专用安装包，不能直接复用另一系统安装出的依赖目录。

1. 阅读“本页面仅用于测试记忆架构”的首次提示；
2. 选择是否保留较早聊天原话、是否允许伴侣先发固定欢迎语；
3. 点击右上角“设置”，填写第三方平台或官方平台的 Base URL 与 API Key；
4. 点击“读取模型”，从平台返回的模型列表中选择；平台不支持模型列表接口时可手动填写模型名；
5. 点击连通测试并保存。API Key 只保存于本机服务端文件，不返回浏览器页面；
6. 主聊天模型必须配置；后台模型可选择“跟随主模型”；Embedding 只有用户主动开启并授权外部处理后才工作。

本机入口：

- 聊天体验页：`http://127.0.0.1:3000`
- 记忆与系统控制台：`http://127.0.0.1:3000/console`
- API 健康检查：`http://127.0.0.1:8787/health/ready`

关闭启动窗口会停止由本次启动的服务。若端口上已经有本项目服务，启动器会复用它，不会再启动重复实例。

如果需要由命令行验货，在 V2 根目录执行：

```bash
npm run setup
npm run doctor
npm test
npm run demo
npm run launch
```

必须实际报告每条命令的结果，并按下面的含义向用户解释：

| 步骤 | 对普通用户的解释 |
| --- | --- |
| `npm run setup` | 准备只保存在本机的配置和控制台依赖 |
| `npm run doctor` | 检查这台电脑是否具备运行条件 |
| `npm test` | 用自动化用例检查核心规则有没有被改坏 |
| `npm run demo` | 在一次性隔离数据库中真实走一遍保存、召回、Token 裁剪、纠正和删除 |
| `npm run launch` | 启动本地聊天页、记忆 API 和控制台，并打开浏览器 |

`npm run demo` 使用一次性隔离数据库，不会把样例记忆写入真实聊天数据。控制台中的部分质量、任务和生产运维卡片仍是明确标注的设计示例；本地聊天、记忆列表、召回、提示词源文件和本机配置来自真实 API。必须区分真实数据与设计示例。

至少用下面的端到端流程验收一次：正常聊天 → 说“请记住，我的猫叫小白” → 换几轮后追问 → 明确纠正 → 在记忆中心删除 → 再次追问。明确的“请记住”由程序直接保存，不额外调用记忆小模型；普通聊天仍调用主模型。隐含信息、摘要和低频整理按开关进入后台模型任务。

失败时先定位环境、端口、依赖或权限问题，不要跳过测试后宣称完成。用户验货结束后，如果不再使用本机服务，指导用户在运行窗口按 `Control + C`，或由你安全停止自己启动的进程。

### 本机安全边界

- `.env` 与 `data/` 不进入分享包或公开仓库；
- `MEMORYOS_LOCAL_COMPANION=true` 只用于明确的本机伴侣体验，默认只允许回环地址；
- 网页和 APP 绝不能持有 `MEMORYOS_AUTH_SECRET`；
- 本机 API Key 位于 `data/local-secrets.json`，不进入分享包、浏览器或公开仓库；
- 后台查看已有内容不调用模型，也不产生模型 Token；
- SQLite/文件运行时只按单实例参考实现对待。

### 可选 Docker 单机体验

只有用户明确需要容器时才运行：

```bash
npm run setup -- --skip-install
docker compose -f compose.local.yaml up --build
```

该 Compose 只绑定 `127.0.0.1` 并启用本机演示代理，不得直接暴露到局域网或公网。结束后停止容器；不要让 Docker 在用户不知情时持续运行。

## 6. 路线 B：接入已有 APP、网页或机器人

这条路线专门用于用户已经有成品或半成品，只想替换、补强或统一原记忆架构的情况。不要要求用户把产品推倒重做。先识别原系统已经做对的部分，再选择最小侵入的接入点。

### 第一步：只读检查目标项目

至少找出：

1. 用户登录和会话校验入口；
2. 用户、虚拟恋人、关系和会话表；
3. 用户消息进入主模型前的位置；
4. 助手流式回复成功或失败后的落库位置；
5. 当前消息权威源和 message ID；
6. 数据库迁移、队列、定时任务和部署入口；
7. Web/APP/机器人 Push 或主动发送能力；
8. 现有测试、日志、隐私和密钥管理方式。

先画出当前链路和拟接入点，再改代码。不要让 Memory Engine 与原产品各保存一份无法同步的聊天真相。

### 第二步：修改前建立可恢复点

只读检查可以先进行；任何代码写入、依赖升级、数据库迁移或部署变更之前，必须建立并验证可恢复点。不能只说“建议备份”然后继续修改。

- 如果项目使用 Git，记录当前分支、提交号和工作区状态；保留所有未提交修改，不得用 reset、checkout 或覆盖操作清除用户文件。
- 根据用户习惯选择新分支、独立工作树或完整项目副本，并告诉用户恢复入口。
- 对现有数据库做带时间戳的备份或快照，同时保存 Schema 与必要数据；至少执行一次可读性检查，重要生产库还要做恢复演练。
- 安全备份服务器环境变量、反向代理、进程管理、定时任务和部署配置；密钥备份不得进入公开仓库、聊天正文或交付截图。
- 盘点上传文件、对象存储、向量库和外部 Provider 中是否存在必须同步迁移的数据。
- 在修改方案中写明回滚触发条件、回滚步骤、预计停机时间和负责人。

如果用户不懂备份，由你根据项目实际环境给出并执行安全步骤；涉及生产数据库快照、停机或覆盖式恢复时，先向用户说明影响并取得明确确认。

### 第三步：选择接入形态

#### 单实例验证

适合内部测试和小规模试运行：产品后端调用便携 Memory API，使用 SQLite + FTS5 + 文件型 Runtime。它仍要求真实身份校验、稳定备份和仅服务端保存密钥。

#### 独立生产服务

适合多用户、横向扩容：

```text
APP / Web / Bot
  → 产品后端（登录、关系校验、短时 Memory Token）
  → 无状态 Memory API
  → PostgreSQL + RLS + Transactional Outbox
  → 独立 Worker / Scheduler
  → 模型、Embedding、对象存储与 Push Provider
```

推荐生产形态是独立 Memory API；嵌入式使用也必须保持相同的领域接口、作用域和删除语义。

### 第四步：已有后端和管理后台怎样处理

先区分两个容易混淆的概念：

- **产品后端**是处理登录、聊天、模型调用和业务数据的服务器代码；
- **管理后台**是运营人员或用户查看设置与数据的页面。

心忆的 Memory API 是记忆服务能力；随项目提供的控制台只是参考管理界面，不要求已有项目再保留一套重复后台。根据目标项目选择以下一种：

1. **优先推荐：保留原产品后端和原管理后台。** 产品后端调用 Memory API；把需要的记忆、授权、成本和任务页面接进原后台的权限与设计系统。
2. **独立技术控制台。** 原业务后台不方便改动时，把心忆控制台作为仅管理员可访问的独立内部工具，并接入原组织登录、RBAC 和审计。
3. **嵌入现有后台。** 复用心忆的界面组件或信息结构，但所有数据仍通过受保护的管理 API 获取。
4. **新项目才使用完整参考控制台作为起点。**

禁止让两个后台分别维护不同的用户、授权、Prompt 或记忆真相；前端隐藏按钮也不能代替服务端权限。目标项目已有成熟后台时，不要为了展示“部署完整”而强行替换它。

### 第五步：APP 与网页前端的边界

记忆检索、模型密钥、身份校验和权威写入都应在服务端完成，不能把 Memory Secret 或长期记忆规则放进 APK、IPA 或浏览器。

但不能一概说“APP 只是前端，所以完全不用管”：

- 如果只改善回复记忆，且原聊天 API 的请求与返回格式保持兼容，APP 可能几乎不用改；
- 如果新增记忆中心、来源查看、纠正、删除、禁记、授权开关或多伴侣共享，APP/Web 需要对应页面和交互；
- 如果启用主动消息，APP 需要 Push 权限、设备令牌、通知设置、点击回跳和去重处理；
- 如果允许 AI 发送第一条消息，前端必须能展示没有用户前置消息的 assistant 事件；
- 流式聊天需要稳定的客户端 message ID、断线重试和最终提交状态，不能把半截回复误当完整证据。

先检查现有前端和后端契约，再决定是否需要改 APP。目标是最小必要改动，不是默认不改，也不是默认重写。

### 第六步：接入聊天回合

标准链路：

```text
POST /v2/turns:prepare
  → 服务端验证短时 Token 与关系
  → 写入前隐私检查
  → 保存消息或权威 message reference
  → 程序召回与 Token 编译
  → 返回 turnId、turnToken、ETag、ContextEnvelope

产品后端调用主聊天模型

POST /v2/turns/{turnId}:commit
  → 携带 turnToken、If-Match 和新的 Idempotency-Key
  → 助手内容外发/落库隐私检查
  → 提交真实 assistant 消息和后台 Outbox

流式失败调用 :fail
```

客户端断线重试必须复用同一 Idempotency-Key；同一个 key 改变请求正文应返回冲突。流式 partial 默认不能作为完整长期证据。

### 第七步：身份接入

- 用户先登录产品自己的后端；
- 产品后端验证用户确实拥有目标 relationship/companion/conversation；
- 产品后端签发短时、固定 `tenant + user + relationship + companion + conversation` 的 Memory Token；
- 请求正文里的 tenant/user 不能扩大 Token 权限；
- 管理 API 使用独立 audience、角色、服务端会话与审计；
- 正式环境设置 `MEMORYOS_DEMO=false`，隐藏演示令牌和兼容写接口。

### 第八步：分阶段开启能力

建议顺序：

1. 消息入口、写入前隐私和回合协议；
2. 显式“记住”、查看、纠正和整条事实遗忘；
3. 获得 `semantic_index` 授权后开启 FTS 影子召回；
4. 小比例把程序召回注入主聊天；
5. 配好 Provider、Schema、预算和审核 sink 后开启候选抽取，先全部 pending；
6. 开启分段摘要；
7. 只有真实评测证明有收益时才开启 Embedding/Hybrid；
8. 开启用户显式触发的 Deep Recall；
9. 开启用户明确创建的单次事务提醒；
10. 周期提醒、适应性分析、Dreaming 和关系主动消息分别独立实施、评测和灰度。

每一步都需要独立 Feature Flag、预算、指标、Kill Switch 和回滚。

## 7. 路线 C：没有现有项目——个人服务器或全新产品

如果没有现有项目，先确认用户究竟需要“自己能用的一套服务”，还是“准备给多位用户使用的完整产品”。当前包已经带一个单用户、单伴侣的本地聊天入口，可以直接体验或作为个人原型；不要把这个测试页面宣传成已经具备公网账号、支付、Push 与海量并发能力的商业前端。

### 个人服务器、单用户

单用户可以减少容量复杂度，但只要服务暴露到互联网，安全要求不能省略：

- 明确还需要网页、APP、机器人还是已有客户端作为聊天入口；
- 使用 “MEMORYOS_DEMO=false”，关闭演示令牌和演示写接口；
- 配置真实长随机密钥、服务端鉴权、HTTPS、域名或安全隧道；
- 数据库和管理控制台不直接暴露公网；
- 配置进程自启动、日志轮转、健康检查、数据库备份和恢复方法；
- SQLite 只用于单进程、有限负载和明确备份的场景；未来增加并发、多个实例或商业用户时迁移 PostgreSQL；
- 主模型、Embedding 和 Push 根据用户是否需要分别配置，不能默认产生外部费用。

### 全新多用户或商业产品

推荐起点：

- TypeScript/Node.js 产品后端；
- HTTP/OpenAPI Memory API；
- PostgreSQL 作为权威数据源；
- Redis 只用于唤醒、限流和可丢缓存，不作为任务唯一真相；
- Transactional Outbox + 独立 Worker；
- pgvector 初期可选；
- 模型、Embedding、Push、对象存储和 KMS 使用适配器；
- 用户端记忆中心与运营后台使用不同权限体系。

先完成注册登录、聊天回合、显式记忆、查看、纠正和删除闭环，再逐层添加摘要、Embedding、深度召回、适应性分析和主动消息。每一层独立评测、灰度和回滚，不要一次开启全部昂贵或高风险能力。

## 8. 不可破坏的领域规则

以下规则必须同时存在于代码、数据库和测试中，不能只写进 Prompt：

1. tenant、user、relationship、companion 和 conversation 来自已验证身份与权威关系图。
2. 任何可能保存、排队或外发的正文先经过 Pre-Persistence Privacy Gate。
3. Claim、Evidence、Revision、Correction、Consent、Tombstone 和 Suppression 是权威记录。
4. 现实、关系设定、角色扮演、虚构、假设、引用和未知不能静默互相提升。
5. 每个可变事实保存“事实何时有效”和“系统何时采用”的双时间。
6. 模型只提出候选、摘要或文案；不返回权限、Scope、数据库状态、游标和发送决定。
7. 当前用户明确纠正立即压过旧事实，并形成原子版本链。
8. 删除先同步隐藏，再异步传播；迟到 Worker、索引重建和旧导入不能复活内容。
9. 摘要、档案、FTS、Vector 和缓存都是可重建投影，不是唯一真相。
10. 一个部署只能有一个原始消息权威源。
11. 用户私有记忆可在记忆中心查看，但不进入任何恋人的聊天召回。
12. 跨关系共享需要绑定具体记忆和目标关系的一次性授权，不能默认共享。
13. 所有上下文、后台任务、重试、租户和费用都有服务端硬上限。
14. 主动消息创建时、生成前、写 Outbox 前和发送前都重新检查最新授权与开关。
15. 队列、Trace、日志、导出和错误报告不能成为私密正文的第二份永久存储。

## 9. 隐私、授权与删除

### 内容处理顺序

```text
认证与请求上限
→ 本地确定性敏感信息检测
→ 用户类别偏好与目的授权
→ 保存/目的地决定
→ 脱敏或加密
→ 才能进入数据库、队列、模型或 Embedding
```

密码、验证码和 API Key 不得进入长期记忆、Embedding、日志或 Job。敏感内容默认待确认；高敏感内容默认不建立 FTS/Vector。

### 目的授权分开

普通记忆、原话档案、语义索引、外部 Embedding、跨关系共享、深度查找、事务提醒、关系主动消息、营销和锁屏正文使用不同 Consent。平台开关只能关闭能力，不能替用户授权；撤回必须立即影响读取路径和未发送任务。

### 删除选项

用户界面必须区分：

- 只忘掉整理出的记忆；
- 同时删除来源聊天和失去证据支持的记忆；
- 以后不要再记这一类内容。

便携 API 当前已验证整条事实遗忘；证据/会话范围删除、完整传播查询和导入导出执行接口仍需要生产适配器，不得返回假成功。

## 10. 提示词与模型接入

- `prompts/registry.seed.json` 是提示词注册表种子；发布版本不可原地修改。
- `core-persona` 和 `main-chat-policy` 属于稳定前缀，尽量保持顺序与字节稳定。
- 近期消息保留真实 `user/assistant/tool` role。
- 记忆、摘要、人物名和导入内容都是不可信数据，不能覆盖系统指令。
- 结构化模型输出先过 JSON Schema、长度、枚举和 Evidence ID 子集检查。
- 无效输出安全失败或进入有限重试；默认不再调用第二个模型修 JSON。
- 每个后台结果记录 prompt、model、policy、config、tokenizer 和费用版本。

本地聊天只有在用户配置主模型并主动发送消息时才调用主模型。后台候选抽取与滚动摘要由程序信号触发；后台模型未单独配置时复用主模型，因此可能产生额外但有界的模型费用。明确匹配“请记住，正文”的内容由程序直接保存，不额外调用记忆小模型。Embedding 必须单独配置、开启并取得外部处理授权；Push 不因本地聊天而自动开启。不得增加一个含糊总开关，让下载后的项目在用户不知情时产生费用。

## 11. 主动消息与提醒

大模型不会自己在未来醒来。正确链路是：

```text
用户或获授权规则创建 Event
→ Scheduler 物化 Occurrence
→ Worker 领取带 fencing token 的租约
→ 复查授权、总开关、类别开关、勿扰和频率
→ 模板或模型生成文案
→ 再次复查
→ Transactional Outbox
→ Push/机器人 Provider
→ 设备去重与状态回写
```

事务提醒、站内引导、关系主动关心和营销分别授权。精确提醒优先确定性模板和设备本地通知；模型不可用时不能让闹钟失效。

当前便携版只验证单次事务提醒。周期 RRULE 展开、交互式创建策略、已有提醒重排和真实 APNs/FCM/WebPush 必须由生产 Scheduler/Provider 实现；在实现前返回 `501`。

## 12. 正式生产必须替换或补齐什么

### PostgreSQL Runtime

- 把 memory/runtime repository 接到 PostgreSQL 事务；
- 数据平面表启用并强制 RLS；
- 每个事务设置 tenant/user 上下文并防连接池污染；
- 使用 Transactional Outbox、fencing token、CAS 游标和严格状态机；
- 把参考 DDL 拆成有序、可回滚的迁移并执行 N-1 → N 验证。

### 基础设施

- OIDC/OAuth、delegated token 和服务间 mTLS；
- KMS/Secret Manager、信封加密和密钥恢复；
- Worker Pool、Scheduler、对象存储、Push Provider；
- 备份恢复、删除 Manifest 和外部 Provider 对账；
- 监控、告警、Runbook、RPO、RTO 和责任人；
- SBOM、依赖扫描、隐私、未成年人和心理安全评审。

### 管理后台

当前控制台的记忆、详情、证据、授权、提醒、召回和八份 Prompt 源文件可以读取真实 API；Job、质量及部分生产运维卡片仍明确标注为设计样例。正式后台需要独立 RBAC、服务端会话、CSRF、防 XSS、JIT 原文审批、审计、分页和真实管理 API。前端隐藏按钮不是权限。

## 13. 迁移与回滚要求

每项生产迁移必须提供：

- dry-run 和目标版本；
- 向前迁移与数据校验；
- 兼容窗口或停机说明；
- 回滚或从备份恢复步骤；
- 派生索引异步重建；
- 删除、授权和 Outbox 的并发验证；
- 真实样本的 N-1 → N 演练。

不要长期双读、双写两套权威 Schema。旧格式只在导入器或一次性迁移中转换。

## 14. 验收与发布门槛

先运行当前工程：

```bash
npm run doctor
npm test
npm run demo
```

不要在部署手册中复制某次历史测试数量。每次修改、迁移或发布都必须重新执行并报告本次真实通过、失败和跳过数量；只有实际执行过 PostgreSQL、Windows、浏览器或容量测试，才能声称对应环境已经验证。

正式生产还必须验证：

- 跨 tenant/user/relationship 泄漏为 0；
- 未授权主动消息为 0；
- 禁存正文进入数据库、队列、日志和外部 Provider 为 0；
- 删除后召回或后台复活为 0；
- 角色扮演/引用自动提升为现实为 0；
- 不属于输入的 Evidence ID 被接受为 0；
- 用户纠正后旧事实作为当前事实使用为 0；
- 核心上下文硬超限为 0；
- 同一 occurrence 用户端重复展示为 0；
- 后台模型自动改写核心人格为 0。

还需报告真实硬件和数据量下的延迟、Recall@K、写入精确率、Token、费用、容量、备份恢复、删除传播、SLO、RPO 和 RTO。

## 15. 完成部署后必须交付

“代码已经构建”不等于交付完成。你必须根据用户的实际电脑、服务器、控制面板和产品形态，给出一份普通用户能够照着使用的说明；不能只回复“构建成功”或留下一串没有解释的命令。

### 15.1 实际环境部署手册

部署手册必须针对本次真实环境生成，至少写清楚：

- 部署在本机、NAS、VPS、云平台还是公司集群；
- 实际操作系统、CPU 架构、运行目录、域名和端口；
- 需要安装的软件、最低版本、安装来源和验证命令；
- 环境变量分别填什么、密钥怎样安全生成和保存；
- 数据库怎样初始化、迁移、备份、验证和恢复；
- 服务怎样启动、停止、重启、开机自启和查看日志；
- 反向代理、HTTPS、DNS、防火墙和仅内网开放的端口；
- 更新新版本与回滚旧版本的逐步方法；
- 部署完成后逐项访问哪个地址、看到什么才算成功。

不要默认所有服务器都使用宝塔面板。宝塔、1Panel、Docker Compose、systemd、PM2、Kubernetes 或云平台托管服务只是不同部署方式，应根据现有环境和用户偏好选择，避免为了使用某个面板而破坏已经工作的部署。

如果用户明确使用宝塔或其他面板：

- 先读取实际系统、面板版本、已安装软件和现有站点配置；
- 再告诉用户需要哪个运行环境、数据库、反向代理或进程管理能力；
- 给出当前官方安装页面或官方插件来源、准确版本和安装后的验证方法；
- 不推荐来源不明的第三方脚本、镜像或插件；
- 不要凭记忆编造已经变化的菜单名称和下载地址，必要时查询当时的官方文档；
- 说明哪些端口只供本机或内网使用，不能直接暴露公网。

如果你无权登录服务器或执行安装，就提供逐步操作、复制命令、预期输出和失败排查，并明确标记“等待用户执行”，不得声称已经部署。

### 15.2 普通用户使用手册

至少告诉用户：

- 聊天入口、用户记忆中心和管理员后台分别从哪里打开；
- 本地包第一次怎样双击启动、阅读提示、配置主模型并完成第一轮聊天；
- 正式产品第一次怎样登录、创建或绑定虚拟伴侣、配置主模型；
- 怎样开启或关闭长期记忆、摘要、Embedding、深度召回和主动消息；
- 怎样查看来源、纠正、删除、禁记和导出；
- 单伴侣和多伴侣时记忆如何隔离，怎样进行选择性共享；
- 如何用“记住—换会话追问—纠正—删除—再次追问”完成第一次验收；
- 如何查看 Token/费用、任务失败、日志和服务健康；
- 日常怎样备份，出现问题怎样恢复或关闭新增能力；
- APP、网页或机器人需要更新时，用户具体安装哪个构建包或发布哪个版本。

### 15.3 技术交付报告

向用户提供一份基于实际环境的交付结果，至少包括：

- 实际拓扑、域名、服务和环境依赖；
- 修改和新增的文件；
- 会话权威源与聊天接入点；
- 数据库迁移、验证和回滚；
- Token、RBAC、RLS 和跨租户结果；
- 原始消息保留、外部处理和授权策略；
- 已发布 Prompt、模型、Embedding 和配置版本；
- Worker、Scheduler、Outbox、Push 与幂等策略；
- 用户记忆中心和管理后台入口；
- 删除、导入导出、备份与密钥恢复；
- Feature Flag、Kill Switch、监控、费用上限、RPO/RTO；
- 实际执行的测试、失败项、已知风险和生产状态。

若还有生产阻断项，必须明确标记“未完成”，不能为了交付完整感而隐藏。最后用普通语言总结“现在能做什么、用户下一步做什么、哪些能力仍未开启”，不要让用户从技术日志里自行猜测。

## 16. 禁止事项

- 不要把全部聊天历史作为普通兜底反复发送给模型。
- 不要把向量库、Markdown、摘要或缓存当作唯一权威记忆。
- 不要让后台模型自动修改核心人格。
- 不要在用户未授权时默认开启主动关心或营销。
- 不要使用内疚、威胁、占有和依赖强化文案逼迫用户回复。
- 不要让精确提醒依赖模型必须在线。
- 不要因为索引故障删除权威记忆。
- 不要让打开或刷新后台页面触发模型调用。
- 不要把服务端签名密钥放进浏览器、APK、IPA、日志或截图。
- 不要信任导入包中的 tenant/user、Consent、向量或生产 Prompt。
- 不要把示例 SQL、Docker 构建或单机健康检查称为生产上线。
- 不要声称测试通过，除非本次实际执行并保留结果。

## 17. 你的最终目标

把这一套稳定的领域内核接入用户真实产品，让普通聊天成本保持有界，记忆有来源、有时间、有关系边界、能纠正、能删除，主动消息得到用户控制；同时对尚未实现或尚未验证的生产能力保持诚实。
