# 心忆 MemoryOS V2：技术架构与部署说明

本文是 V2 的完整技术参考，统一说明领域模型、数据流、Token 策略、隐私删除、主动消息、代码结构、部署档位、成本、测试和生产边界。机器字段与接口以 `contracts/` 为准，实际行为以源码和测试为准。

## 1. 目标与设计原则

心忆服务于长期陪伴型虚拟恋人 APP、网页聊天和机器人。它追求的不是“尽量多记”，而是让一条记忆可以回答：

- 谁说的、说的是谁；
- 属于现实、共同关系设定、角色扮演还是引用；
- 事实何时有效、系统何时得知；
- 有什么证据、可信程度如何；
- 哪个用户、关系和虚拟恋人可以使用；
- 怎样纠正、过期、删除和阻止重新学习；
- 何时值得放进本轮上下文，最多占多少 Token。

核心原则：

1. 权威事实与派生索引分离。
2. 模型理解语言，程序决定权限和状态。
3. 单轮上下文有硬上限，不随历史线性增长。
4. 现实、关系设定和扮演不混用。
5. 用户纠正优先，删除立即生效并防复活。
6. 高风险能力默认关闭；高成本能力必须有阈值、独立开关、用量记录和降级路径。
7. 主动消息由事件与授权驱动，不由模型自行发送。
8. 所有外部 Provider 都可替换，领域协议保持稳定。

## 2. 当前交付状态

V2 是一个项目，不是两套内核：

- **便携层**已经可执行，用 Node.js、SQLite 和文件型 Runtime 实现核心协议。
- **生产层**提供机器契约、参考 SQL 和适配边界，需要结合目标产品完成基础设施接入。

### 2.1 能力矩阵

| 能力 | 当前状态 | 边界 |
| --- | --- | --- |
| Claim / Evidence / Revision | 已验证 | 显式记忆带证据和版本 |
| Realm / Attribution / 双时间 | 已验证 | 现实、设定、扮演、引用等分别处理 |
| 纠正与整条事实遗忘 | 已验证 | 旧版本保留历史；删除立即隐藏并防复活 |
| 普通召回 | 已验证 | 实体、FTS、短词兜底、RRF；不调用生成模型 |
| ContextEnvelope | 已验证 | 不可信数据编码和保守 Token 硬预算 |
| 近期原文有界保留 | 已验证 | Runtime 最多保留最近 80 条合格事件；本地聊天编译器最多取 20 条，再按 Token 硬预算裁剪 |
| 深度查找 | 已验证 | 单独授权；当前扫描结构化记忆和获准档案，不调用模型 |
| Turn Prepare/Commit/Fail | 已验证 | action token、ETag、幂等和流式失败边界 |
| 自动候选/摘要信号 | 已验证 | 程序决定是否排任务，寒暄不排模型任务 |
| 本地伴侣聊天页 | 已实现 | 单用户、单伴侣测试外壳；主模型调用、Token 回执、设置和真实消息闭环 |
| 显式“记住”程序路径 | 已实现 | 可安全截取的明确正文不额外调用记忆模型；仍经过 Consent、Privacy、Scope 和 Suppression |
| 小模型 Worker | 已实现并接入本地 Provider | 候选 sink、摘要 sink、预算、重试、死信、fencing 均落地；正式多用户需独立 Worker Pool |
| 滚动摘要持久化 | 已实现 | 固定消息范围、覆盖摘要、游标和 Context 注入；模型失败不阻塞聊天 |
| Embedding Adapter | 已实现、可选 | 本地 JSON 向量投影与 OpenAI-compatible API 已接通；生产需 pgvector/专用向量层 |
| 表达偏好层 | 已实现基础路径 | 用户直接要求可由程序形成 60 天受限偏好；复杂跨日模型提议默认不自动发布 |
| Consent Challenge | 已验证 | 目的、资源、政策版本、一次性 nonce 和过期受程序约束 |
| 单次事务提醒 | 已验证 | 授权、开关、DST、勿扰、过期、迟到和发送前复核 |
| 周期提醒/提醒重排 | 未提供 | 便携 API 返回 501；需生产 Occurrence Scheduler |
| 主动消息发送 | 验证到 mock Provider | 真实 APNs/FCM/WebPush/机器人 Provider 待接 |
| 关系主动关心 | 已实现规则、默认关闭 | 真实心理安全评测和发送 Provider 未完成 |
| 控制台 | 已验证到只读实时连接 | 记忆/证据/授权/召回和 8 份 Prompt 为真实读取；Job、质量页仍含设计样例 |
| 导入导出执行接口 | 未提供 | Schema/SQL 已定义，执行与对象存储适配待接 |
| PostgreSQL + RLS + Outbox | 参考契约 | DDL 已在 PostgreSQL 16 执行；Runtime 与迁移包待接 |

### 2.2 自动化与实机验收

`npm test` 是当前唯一可信的测试数量来源，因为新增能力会同步新增测试。它依次执行 Memory Core、Runtime、HTTP API、本地伴侣编排、JavaScript Client、机器契约、Console SSR、ESLint 和生产构建。

本次实现环境为 macOS；Windows 脚本已做静态兼容和 Node/依赖引导，但未在真实 Windows 10 机器实测。Docker 是可选部署参考，本地双击体验不启动 Docker。任何历史报告中的浏览器、Docker 或测试数字都不能替代本次命令的实际输出。

这些结果只证明当前声明范围，不等于目标产品已经完成生产部署。

## 3. 五个架构平面

```text
权威数据平面
  Messages / Claims / Evidence / Revisions / Corrections
  Consent / Tombstone / Suppression / Proactive Event

派生投影平面
  User Profile / Relationship Profile / Adaptive Profile
  Segment Summary / Conversation State / FTS / Vector / Cache

上下文编译平面
  Trusted Scope / Recall Router / Policy Filter
  Token Budget / ContextEnvelope / Provider Adapter

主动互动平面
  Scheduler / Occurrence / Policy Gate / Generation
  Transactional Outbox / Delivery Provider

控制平面
  User Memory Center / Admin Console / Prompt Registry
  Audit / Cost / Evaluation / Kill Switch
```

只有权威数据平面保存不可替代的业务状态。档案、摘要、时间线、FTS、向量和缓存均为投影，损坏后应能重建。

## 4. 记忆分层

| 层 | 例子 | 规则 |
| --- | --- | --- |
| 核心人格 | 身份、价值边界、基础说话方式 | 产品或用户明确配置；后台模型不得改写 |
| 适应性档案 | 偏好短回复、少问问题 | 直接指令可由程序形成受限、60 天偏好；复杂推断需多条证据与审核 |
| 语义 Claim | 姓名、偏好、边界、人物关系 | 带证据、作用域、Realm、Attribution 和双时间 |
| 关系 Canon | 纪念日、共同约定、虚拟关系经历 | 独立 Realm，不冒充现实 |
| 情节记忆 | 争执、修复、重要经历 | 事件时间线与证据 |
| 近期对白 | 当前会话最近消息 | Token 窗口，不永久增长 |
| 会话状态 | 当前主题、未完成问题 | 可重建摘要，不代替正式事实 |
| 原话档案 | 用户选择保留的旧聊天 | 按保留策略供举证和深度查找 |
| 前瞻任务 | 起床提醒、周年任务 | 独立调度状态机，不塞进长期 Prompt |

## 5. 权威领域模型

### 5.1 Claim、Revision 与 Evidence

- `Claim`：一个逻辑记忆命题。
- `Revision`：Claim 在某个时间段采用的具体版本。
- `Evidence`：指向原消息、说话角色、片段、序号和内容指纹的证据。
- `Correction`：用户纠正或版本替换事件。
- `Consent`：某个目的、资源和政策版本的授权事件与当前投影。
- `Tombstone`：删除事实，确保恢复和导入时先知道什么不能复活。
- `Suppression`：不保存原文的防重新学习规则。

摘要和模型输出只能提出候选，不能直接成为无证据的权威事实。

### 5.2 Realm

```text
real_world           用户报告的现实生活
relationship_canon   用户与虚拟恋人的共同关系设定
roleplay             临时角色扮演
fictional            小说、故事和世界观
hypothetical         假设或举例
quoted               引用他人的话
unknown              无法可靠判断
```

`roleplay`、`quoted`、`fictional` 和 `unknown` 不得由模型静默提升为 `real_world`。

### 5.3 Attribution 与 Epistemic Basis

系统分别记录是谁断言、断言指向谁、证据基础是什么。例如“老板说我没能力”证明用户报告过老板的话，不证明“用户没有能力”。助手建议、Tool Result 和系统内容不能单独证明用户现实事实。

### 5.4 双时间

每个可变事实同时保存：

- `valid_from / valid_to`：事实在用户世界中何时有效；
- `system_from / system_to`：系统何时采用这个版本。

搬家、换工作或偏好变化不会静默覆盖历史，而是创建新 Revision 并关闭旧版本区间。因此既能回答“现在”，也能回答“当时”。

### 5.5 Scope 与关系授权

每次访问绑定：

```text
tenant + user + relationship + companion + conversation
```

这些值来自验证后的 Token 与权威关系图，不来自模型或请求正文。Claim 的来源关系只说明在哪里产生；聊天能否使用由独立 Relationship Grant 决定：

- 用户私有：记忆中心可见，聊天召回不可见；
- 当前关系：只有来源关系可用；
- 跨关系共享：用户对具体记忆和具体目标关系单独授权。

撤销 Grant 或 Consent 后，目标关系立即不再召回。

## 6. 原始消息的唯一权威源

部署必须固定一种模式：

1. **引擎拥有**：Memory Engine 保存消息并分配 message ID。
2. **外部拥有**：产品数据库保存消息，Memory Engine 只保存带 hash/revision 的 message reference，通过 `ConversationArchivePort` 读取。

运行时不得在两种模式之间自动双写或模糊回退，否则会出现删除、纠正和深度查找的第二份真相。

## 7. 同步聊天回合

```text
POST /v2/turns:prepare
  1. 验证 Bearer Token、请求大小、速率和 Idempotency-Key
  2. 解析可信 Scope
  3. Pre-Persistence Privacy Gate
  4. 保存脱敏消息或权威 message reference
  5. 生成 turnId、turnToken、ETag 和 contextRevision
  6. 计算主模型 Token 预算
  7. Recall Router 选择 none / exact / hybrid / deep
  8. 候选回权威库复核
  9. Context Compiler 输出 ContextEnvelope

产品后端调用主聊天模型；本地测试页由 `local-companion.js` 完成同一编排

POST /v2/turns/{turnId}:commit
 10. 复核 action token、If-Match 与幂等键
 11. 对 assistant/tool 内容执行外发与落库隐私检查
 12. 保存真实 assistant 消息或 reference
 13. 原子写 TurnCommitted Outbox
 14. 后台异步处理
```

流式失败使用 `:fail`。Partial 内容默认不进入长期抽取。工具成功前，模型不得声称已经保存、删除或创建提醒。

## 8. ContextEnvelope 与 Token 编译

ContextEnvelope 将不同可信度的资料分开：

```text
安全与产品规则
核心人格版本
短用户/关系/适应性投影
会话状态或分段摘要
少量召回记忆（明确标记为不可信数据）
近期真实 role 对话
当前用户消息
```

可用输入预算：

```text
模型上下文上限
- 输出预留
- Tool Schema
- Provider 包装开销
- 安全余量
= 可分配输入
```

超预算时依次删除低相关记忆、较旧对白、会话状态中的非关键表达和低重要投影。始终保留安全规则、核心人格关键部分、用户当前消息、当前明确纠正、明确关系边界和理解指代所需的最近消息。

默认计数器是保守估算；生产必须接目标模型的精确 tokenizer 并再次硬裁剪。

### 稳定前缀与 Prompt Cache

稳定前缀按固定顺序保存产品安全规则、核心人格和 Tool Schema；动态档案、摘要、记忆和对白放在后缀。核心人格与工具集合在会话内冻结，列表和 JSON 采用确定顺序，减少无意义的缓存失效。

## 9. 零散信息如何成为记忆

```text
TurnCommitted
→ Signal Detector（程序）
→ Batch Planner（程序持有消息范围和游标）
→ 小模型提出 MemoryCandidate
→ JSON Schema 与 Evidence ID 子集验证
→ 隐私与 Consent
→ Realm / Attribution
→ Suppression
→ 去重、时间冲突与敏感度
→ active / pending / rejected / temporary
→ Index Outbox
```

程序触发条件可以是：累积有效用户消息、达到 Token 阈值、主题结束或无法直接处理的明确“记住”信号。普通寒暄和表情不应唤醒抽取模型。

本地实现先走一条更便宜的显式路径：当消息完整匹配“请记住，正文”且正文边界清楚时，程序截取正文并建立 explicit Claim，不额外调用记忆小模型。含糊的“记住刚才那件事”、敏感内容、冲突和隐含偏好仍进入受控候选或确认流程。已经由程序直接捕获的事件带 `memoryCapturedByProgram` 元数据，不会再次触发同一条模型抽取。

模型不能返回 tenant、user、scope、权限、数据库状态或处理游标。Sink 成功后程序才推进 checkpoint；模型或 Sink 失败不会阻塞主聊天。

## 10. 分段摘要、Dreaming 与适应性档案

### 分段摘要

程序选择固定、连续、非重叠的消息范围；模型只总结内容并引用输入 message ID。覆盖范围、sequence、hash 和游标由程序写入。同一范围不反复总结，除非来源删除、Prompt 升级或明确重建。

Conversation State 每若干段从原始 Segment Summary 重建，避免反复改写同一自由文本导致事实漂移。摘要只负责连贯，不代替长期 Claim。

### Dreaming

Dreaming 是低峰期的离线巩固任务：去重、冲突提示、时间整理和长期价值建议。程序先冻结范围；模型不能扩大权限、删除证据或直接发布高影响记忆。当前仅提供 Prompt、任务端口和安全规则，默认不自动发布。

### 适应性档案

交流长度、提问频率、温暖度、直接程度、幽默和表情密度可以进入独立适应层；核心人格、价值边界和关系承诺不能被后台模型改写。

当前本地路径只识别“以后回复短一点”“别总问问题”等用户直接指令，保存 evidence message ID 和 60 天过期时间，并从下一轮开始注入很小的 `adaptive_expression_profile`。这一步由程序完成，不产生额外生成调用。`adaptive-profile.v2.yaml` 用于更复杂的跨多日提议，但默认不自动发布，正式产品应增加待确认、撤回和历史版本 UI。

## 11. Recall Router

| 层级 | 条件 | 额外模型费用 |
| --- | --- | --- |
| Tier 0 / none | 寒暄、表情、简单确认 | 无 |
| Tier 1 / exact | 姓名、日期、称呼、明确关键词 | 无 |
| Tier 2 / hybrid | 主题问题且 FTS 不足 | 可有一次 query Embedding |
| Tier 3 / rerank | 特殊高价值场景 | 默认关闭 |
| Tier 4 / deep | 用户明确查历史或受控升级 | 当前便携路径无模型；生产可选一次证据整理 |

普通召回顺序：

1. 实体与别名；
2. 获授权的 FTS5 trigram；
3. 中文 1–2 字的 scope-bound 有界 LIKE；
4. 可选向量候选；
5. RRF 合并不同排名；
6. 回权威 Claim 检查 Scope、Realm、Consent、时间、当前 Revision、Tombstone 和 Suppression；
7. Token Compiler 选入 ContextEnvelope。

BM25 与 cosine 不直接相加。Recall Trace 默认只记录指纹、候选 ID、分数、过滤原因、配置版本和用量，不保存完整私密查询。

Deep Recall 依次查结构化 Claim、分段摘要和用户仍选择保留的原话档案。它有候选数、片段数、Token、超时、每用户次数和租户金额上限；找不到证据时必须坦诚返回未找到。

## 12. 写入前隐私与授权

### 12.1 Privacy Gate

```text
认证与限流
→ 本地确定性 DLP
→ 用户类别偏好与目的 Consent
→ storage decision
→ destination decision
→ 脱敏或加密
→ 数据库 / Queue / Model / Embedding
```

| 内容 | 原文 | 长期记忆 | FTS/Embedding | 日志 |
| --- | --- | --- | --- | --- |
| 普通内容 | 按保留策略 | 可候选 | 用户允许时 | ID/指纹 |
| 敏感内容 | 加密、短保留 | 默认待确认 | 默认关闭 | 禁止正文 |
| 高敏感内容 | 显式同意 | 严格确认 | 默认关闭 | 禁止正文 |
| 密码/验证码/API Key | 不持久化或立即脱敏 | 禁止 | 禁止 | 禁止 |

用户输入、assistant 回显和 Tool Result 分别生成与 tenant/user/relationship/conversation/role/content-HMAC 绑定的一次性 Privacy Receipt，不能跨内容和角色复用。

### 12.2 Consent

不同目的分别授权：聊天处理、普通/敏感记忆、原话档案、语义索引、外部 Embedding、外部记忆 Provider、跨关系共享、深度查找、事务提醒、站内引导、关系主动、营销、适应性分析和锁屏正文。

Grant 消费短时、一次性、绑定目的/资源/关系/政策版本/披露摘要的 Challenge。服务端只保存 keyed HMAC，不保存可恢复的 Bearer Token。Withdraw 不要求新 Challenge；事件与当前投影原子更新，旧 Revision 不能覆盖新决定。

平台开关、租户策略和用户授权是三层闸门：前两层只能进一步关闭，不能代替用户同意。

## 13. 纠正、删除与防复活

### 13.1 纠正

```text
锁定旧 Revision
→ 验证 Evidence 与 If-Match
→ 写 Correction Event
→ 关闭旧 system_to
→ 创建新 Revision
→ 更新投影与 Outbox
```

通用 PATCH 不能把 deleted、rejected 或 superseded 任意改回 active。

### 13.2 删除范围

用户界面区分：

1. 忘掉整理出的记忆；
2. 同时删除来源消息和仅由其支持的记忆；
3. 以后不要再记这类内容。

同步事务先验证身份与版本、提升 deletion epoch、写 Tombstone/Suppression、让正常读取不可见、取消未发送任务并写 Deletion Outbox。

异步传播到：

```text
Profile / Summary / FTS / Vector / Cache
Prompt Debug Artifact / External Provider
Pending Outbound / Export Object / Backup Manifest
```

每步记录 receipt，失败重试但不得恢复可见状态。

Suppression 可按 Claim、来源消息、类别、subject+predicate、来源对象或 tenant-keyed HMAC 生效。规范化算法和 HMAC key 版本必须记录；不能通过去掉否定词或数字把不同事实错误合并。备份恢复与导入先重放 Tombstone/Suppression，再处理 active 数据。

## 14. 导出与导入

导出默认包含 Claim、Revision、Evidence 引用、Correction、关系事件、适应性档案、Tombstone/Suppression 标识和 Prompt 元数据；原始聊天由用户显式选择。

安全要求包括 step-up auth、短期下载 URL、加密签名、每文件唯一 AEAD nonce、Manifest 规范化签名、路径/nonce 唯一校验、下载次数和对象生命周期。不能使用可猜明文的普通 SHA-256 作为公开清单指纹。

导入流程：

```text
隔离上传
→ Manifest / Schema / Signature
→ 路径穿越与解压炸弹检查
→ 当前身份重建 tenant/user
→ 用户确认来源关系到目标关系映射
→ Tombstone/Suppression 优先
→ Active Records
→ 当前隐私与 Consent
→ Realm/Attribution
→ 冲突预览与确认
→ 重建投影
```

不能导入向量、缓存、密钥、API Key、生产 Prompt 或旧服务 Consent，也不能把 imported/unknown 自动标记为用户确认。

## 15. 主动消息与提醒

Event 是长期日程，Occurrence 是某次具体发送。两者状态分离：

```text
Event: scheduled / paused / completed / cancelled / expired / failed

Occurrence:
scheduled → leased → policy_checked → generated
→ outbox_committed → provider_accepted → delivered → opened → completed
```

完整链路：

```text
Event Created
→ 确定性校验
→ Occurrence Materialization
→ Scheduler Lease（fencing token + expiry）
→ 最新 Consent / 设置 / 勿扰 / 频率
→ 模板或主动文案模型
→ 安全检查
→ 再次读取最新政策
→ Transactional Outbox
→ 短时 Delivery Authorization
→ Provider + Device Deduplication
→ 状态与 assistant/proactive_outbound 消息回写
```

基础设施允许 at-least-once，用户可见效果依靠 `(event, occurrenceKey)` 和 `(occurrence, channel)` 唯一键做到 at-most-once。旧 Worker 的 fencing token 失效后不能提交。

只有明确的事务提醒可以选择穿过勿扰；关系主动消息和营销不能绕过。模型故障时精确提醒使用批准模板，关系消息则跳过。用户关闭或撤回在 Provider submitted 前必须优先；Provider 已接收后只能按渠道真实能力取消，UI 不得假装已经追回。

## 16. Prompt Registry

`prompts/registry.seed.json` 管理版本化 Prompt：

- 发布后的 `(promptId, version, digest)` 不可修改；
- 输入/输出 Schema、模型等级、默认开关、负责人和费用可审计；
- 草稿、测试、灰度、发布和回滚分离；
- 生产原文和密钥不进入 Prompt 后台；
- 小模型输出必须通过结构化契约和领域复核；
- 额外模型调用均受独立开关、程序阈值和 Provider 可用性约束；显式记忆不依赖它们。外部 Embedding、复杂适应分析和关系主动默认关闭。

Prompt 不是权限系统。XML 标签或黑名单不能代替真实 role、Tool Schema、认证、Scope 和数据库约束。

## 17. 控制台与用户界面

### 17.1 当前控制台

`apps/console` 现在同时提供本地测试聊天页和记忆控制台。

聊天页 `/` 提供：

- 首次用途与本机归档提示；
- 伴侣先发第一条固定欢迎，0 Token；
- 默认“心忆”人设及可编辑人设字段；
- 主模型、后台模型和独立 Embedding 配置；
- 模型发现、手填回退和真实连通性测试；
- 近期对白、召回、摘要、适应层和主模型的端到端聊天；
- 手动深度回忆和本轮 Token 回执。

控制台 `/console` 提供：

- 演示数据模式；
- 实时 API 总览；
- 记忆列表、详情、历史和 Evidence；
- 关系时间线；
- Consent 与主动设置；
- 单次提醒只读状态；
- 真实 `/v2/context:compile` 召回调试和 Token 展示。
- 8 份 `prompts/` 源文件的完整只读预览。

Job 队列和质量页当前仍是设计样例；高风险写操作在正式 RBAC/JIT/审计完成前保持禁用。控制台本机启动时会自动申请回环 Demo Token 并连接同一固定单用户作用域；公网生产必须移除这条便捷路径。

### 17.2 正式用户记忆中心

应展示记忆正文、类型、确认状态、Realm、Scope、来源、有效时间、当前/历史状态，并支持纠正 Realm/Attribution、修改共享、删除范围、待确认收件箱、隐私开关和主动消息管理。

### 17.3 正式运营后台

默认只看聚合指标、队列、成本、Provider 健康、召回质量、Prompt 版本和删除传播。查看亲密原文必须 step-up auth、用途、审批、短时 JIT 权限和审计。UI 隐藏不是权限，服务端每次仍执行 Scope/RBAC/RLS。

### 17.4 交互要求

320px 到桌面响应式、键盘可用、清晰焦点、颜色不是唯一状态、完整 Loading/Empty/Error/Degraded/Offline/Permission/Deletion Pending 状态。高风险操作使用明确范围和回执，不使用恐吓或操纵文案。

## 18. 成本模型

成本分开记录：主聊天、候选抽取、摘要、Embedding、深度整理、适应性分析、主动文案、数据库、队列、对象存储和推送。

建议输入比例起点：

| 层 | 可分配输入占比 |
| --- | --- |
| 安全 + 核心人格 | 10%–18% |
| 用户/关系/适应性短投影 | 5%–10% |
| 会话状态/摘要 | 5%–10% |
| 召回记忆 | 6%–12% |
| 近期原话 | 剩余主要空间 |
| 当前消息 | 单独硬上限并优先保留 |

候选抽取按批次触发；同一摘要范围只总结一次；Embedding 只处理新增或变更且获准的数据；主动 occurrence 只生成一次，投递重试复用正文与 message ID。

费用熔断顺序：关闭 LLM Rerank/Query Expansion、Hybrid 降级 FTS、暂停低价值整理、主动文案退模板、延后 Embedding。不能为了省钱关闭鉴权、Consent、删除传播和隐私检查。

长期成本验收使用 100、5,000、50,000 turn 数据，检查普通回合输入 P95 不随历史线性增长，并按用户、租户、能力和 Provider 报告 Token、费用、延迟与熔断。

## 19. 后台任务与故障降级

Job 状态：

```text
pending → leased(fencing_token) → running → succeeded
旁路：retry_wait / dead_letter / cancelled
```

Job Payload 只保存 Scope 与对象 ID。幂等键覆盖 tenant、job type、source revision 和 batch range；只有结果事务成功后才推进 checkpoint。

| 故障 | 降级 |
| --- | --- |
| 小模型失败 | 有限重试/死信，保留权威消息，不阻塞聊天 |
| Embedding/Vector 失败 | 降级实体/FTS并告警 |
| Summary 失败 | 使用上一投影和受限近期消息 |
| Deep Recall 超时 | 返回未找到或稍后查询，不编造 |
| Redis/Queue 丢失 | 从数据库 Outbox 恢复 |
| 主动文案失败 | 精确提醒用模板；关系消息跳过 |
| Push 失败 | 复用同一 message ID 有限重试 |
| 数据库不可写 | 明确失败，不假装保存成功 |

## 20. 代码结构与可替换端口

```text
apps/api/src/
  auth.js             Bearer/action token 与角色验证
  context.js          便携上下文编译与本地时间解析
  http.js             HTTP 路由、安全头、契约适配
  local-companion.js  本地人设/Provider/聊天/摘要/向量/表达偏好编排
  runtime-bridge.js   Memory Core 与 Runtime 桥接
  server.js           环境配置与服务入口

packages/memory-core/src/
  repository.js       SQLite 权威记忆仓储与事务
  schema.js           迁移和表结构
  privacy.js          写前隐私策略
  context-envelope.js 上下文预算与不可信编码
  rrf.js              多路排名融合

packages/runtime/src/
  turns.js            Prepare/Commit/Fail
  repository.js       文件/内存运行时仓储
  signals.js          程序信号与批次规划
  workers.js          Durable Job Worker
  proactive.js        主动消息与 Outbox
  adapters.js         模型/Embedding/Provider 端口
  cost.js             费用硬预算
  resilience.js       降级与 Circuit Breaker

packages/client-js/   fetch SDK
apps/console/app/     本地聊天、模型设置、控制台与实时 API 连接
config/               默认伴侣配置
contracts/            OpenAPI、JSON Schema、PostgreSQL DDL
prompts/              Prompt 源文件与 Registry Seed
```

生产实现至少保持这些端口：MessageStore、ClaimStore、EvidenceStore、CorrectionStore、ConsentStore、SuppressionStore、ConversationArchivePort、LexicalSearchPort、VectorSearchPort、EmbeddingPort、MemoryModelPort、PromptRegistryPort、TaskQueuePort、SchedulerPort、NotificationPort、KeyManagementPort、ExportImportPort 和 AuditPort。

适配器不得绕过领域不变量；外部记忆服务只能是派生目标，召回结果必须回权威库复核。

## 21. API 与机器契约

- `contracts/openapi.yaml`：HTTP API，OpenAPI 3.1。
- `ContextEnvelope.schema.json`：主模型输入包。
- `MemoryCandidate.schema.json` 与 `MemoryExtractionResult.schema.json`：小模型候选。
- `RecallTrace.schema.json`：脱敏召回追踪。
- `ProactiveEvent.schema.json`：主动事件与授权快照。
- `ExportManifest.schema.json`：导出包清单。
- `postgresql-production.sql`：生产 Schema/RLS/状态机参考。

JSON Schema 之后仍需领域校验：Evidence ID 必须属于输入集合；文件路径与 nonce 跨条目唯一；时间先后、RRULE、作用域、Consent 和状态迁移由服务端验证。客户端和 UI 类型应从权威契约生成或做兼容性测试，不能手抄另一套枚举。

本地测试外壳另有一组仅回环开放的 `/local/v2/*` 扩展：配置、人设、Provider 模型发现/测试、聊天、消息列表、后台任务手动运行、候选和 Prompt 源文件。它们要求绑定 `memory_admin` 的短时 Token，不属于面向公网客户端的稳定 OpenAPI 契约。迁移到正式 APP 时，产品可以继续使用标准 `/v2/turns:prepare`、`:commit`、记忆和设置接口，或在自己的后端实现等价编排。

本地 UI 固定一个私人伴侣，以减少普通用户的选择负担；底层 Scope 仍保留 `tenantId + userId + relationshipId + companionId + conversationId`，所以未来接入多用户或多伴侣无需重写 Claim 和隔离规则。

## 22. 便携部署

### 22.1 环境变量

```text
MEMORYOS_DEMO
MEMORYOS_LOCAL_COMPANION
MEMORYOS_HOST
MEMORYOS_PORT
MEMORYOS_DB_PATH
MEMORYOS_RUNTIME_PATH
MEMORYOS_LOCAL_CONFIG_PATH
MEMORYOS_LOCAL_SECRETS_PATH
MEMORYOS_LOCAL_STATE_PATH
MEMORYOS_AUTH_SECRET
MEMORYOS_FINGERPRINT_KEY
MEMORYOS_ALLOWED_ORIGIN
```

非演示模式的两个服务密钥至少 32 字节，且必须分别生成。CORS 只允许明确 Origin，不使用 `*`。`MEMORYOS_LOCAL_COMPANION=true` 只允许绑定 loopback；除非运维人员显式设置 `MEMORYOS_LOCAL_ALLOW_PROXY=true` 并在外层提供正式认证，否则服务拒绝在非回环地址启动。

本地 Provider 密钥保存在 `MEMORYOS_LOCAL_SECRETS_PATH`，接口只返回 `apiKeySet`。该文件方案只适用于单机体验；服务器生产使用 KMS/Secret Manager，不能把每位用户的密钥集中明文保存在共享 JSON。

### 22.2 SQLite

- foreign keys、WAL、busy timeout；
- 单写者或数据库租约；
- Durable Job 表而非纯内存定时器；
- 数据库/WAL/SHM 和 `.env` 仅当前用户可读写；
- Keychain/Keystore 管理数据库密钥；
- 摘要、FTS 和其他投影可重建。

本地首次确认后的默认开关：长期记忆、语义全文索引、本机原话档案、程序显式记忆、候选阈值、滚动摘要、受限深度查找、直接表达偏好和第一条站内欢迎开启；后台生成任务只有主/后台 Provider 可用时才真正启用。外部 Embedding、Dreaming 自动发布和关系型主动消息关闭。所有默认值都能在 UI 查看；外部 Embedding 必须同时打开独立披露开关。

### 22.3 Docker

`compose.local.yaml` 是回环地址单机体验模板。它启用 demo proxy 是因为 Docker NAT 会改变来源地址；只有端口固定在 `127.0.0.1` 时才可使用。任何公网/局域网部署必须关闭 Demo 并使用真实身份系统。

## 23. PostgreSQL 正式生产

推荐拓扑：

```text
Load Balancer
  └─ Stateless Memory API

PostgreSQL
  ├─ Canonical Messages / Claims / Consent / Events
  ├─ Forced RLS + pg_trgm
  ├─ Transactional Outbox
  └─ pgvector（可选）

Redis
  └─ 唤醒、限流、短缓存；不是任务唯一真相

Worker Pools
  Capture / Summary / Index / Delete / Export / Proactive

KMS / Object Storage / Push Providers / Monitoring
```

要求：

- API 每个事务设置并清理 tenant/user session context；
- Consent、Correction、Delete 读主库，不依赖延迟副本；
- Job Payload 不复制聊天正文；
- 向量 ANN 前执行 Scope Filter，返回后回权威库复核；
- Embedding 记录 provider/model/dimension/input hash/policy version；
- 模型或维度变化创建新索引版本并原子切换；
- 高敏感正文使用应用层信封加密；
- Secret、Push 凭据和 data key 进入 Secret Manager/KMS；
- 有轮换、吊销、备份恢复和离职权限回收流程。

参考 SQL 不是最终迁移包。必须按目标 PostgreSQL/扩展版本拆分迁移、回滚和恢复步骤。

## 24. 部署顺序、迁移与回滚

推荐顺序：

1. 冻结会话权威源、身份和关系图。
2. 冻结 Schema、配置、Prompt 和保留政策。
3. 部署迁移与 RLS，执行跨租户测试。
4. 部署 API，只开显式记忆与删除。
5. FTS 影子召回，再小比例注入。
6. 候选影子抽取，先 pending。
7. 分段摘要。
8. Embedding/Hybrid 小比例。
9. 用户触发 Deep Recall。
10. 单次事务提醒。
11. 周期提醒、Adaptive、Dreaming、关系主动分别灰度。

每项迁移包含 dry-run、数据校验、兼容窗口、回滚/恢复、索引重建和 N-1 → N 实样演练。不得长期保留模糊双读双写。

## 25. 生产验收与发布阻断

任一情况出现即阻断上线：

- 跨租户/用户/关系泄漏；
- 未授权主动消息；
- 禁存内容进入数据库、队列、日志或禁止 Provider；
- 删除后正常召回或后台复活；
- 扮演、引用或虚构自动提升为现实；
- 模型扩大 Scope、改变 Consent、推进 Cursor 或伪造 Evidence；
- 用户纠正后旧事实仍作为当前事实；
- 核心上下文硬超限；
- 同一 Occurrence 用户端重复展示；
- 核心人格被后台模型自动改写；
- 管理员默认可读亲密原文；
- 没有备份/密钥恢复和删除传播路径。

正式报告还应包含代码、Schema、Prompt、模型、Embedding、Tokenizer、配置、语料版本，真实数据量和硬件，指标样本量与失败案例，成本、容量、SLO、RPO/RTO、Owner、Kill Switch 和回滚版本。

建议质量目标不是未经测试的承诺：普通写入精确率 ≥98%、关键记忆 Recall@K ≥92%、显式记忆成功率 ≥99.9%、Deep Recall found 结果 100% 有证据、5,000 与 50,000 turn 用户普通输入 P95 差异 ≤10%。必须用目标产品真实语料重新评测。

## 26. 仍需完成的生产工作

1. PostgreSQL Memory/Runtime Repository、正式 Outbox 与有序迁移。
2. 真实 OIDC、关系图、mTLS、KMS 和 Secret Manager。
3. 独立 Worker Pool、生产级小模型/Embedding Provider、精确 tokenizer 和费用账本。
4. 候选审核/发布、摘要与向量的 PostgreSQL/向量库持久实现及重建工具。
5. APNs/FCM/WebPush/机器人 Provider 和 Push Token 生命周期。
6. 周期提醒、重排、Provider 已接收后的真实取消边界。
7. 证据/范围删除、传播查询、导入导出和对象存储。
8. 通用写接口的持久幂等响应账本。
9. 生产管理端 RBAC、JIT Reveal、审计与真实 Job/指标 API。
10. 多实例竞态、跨关系共享、容量、成本、安全、未成年人、心理安全和合规验收。
11. 在所有源码与发布产物中保留 MIT 许可证和版权声明。

## 27. 来源、许可与准确表述

研究来源的版本、许可证和文件摘要保存在 `SOURCE_PROVENANCE.json`。V2 运行时代码是独立的多租户领域实现，并以根目录 `LICENSE` 中的 MIT License 对外许可。来源记录只用于说明研究脉络，不改变 HeartMemoryOS 自身的许可条款。

可以准确表述：

> 心忆 MemoryOS V2 已完成一套可运行、可测试、可迁移的长期陪伴记忆参考架构。普通召回和后台查看不额外调用生成模型；正式海量用户部署仍需结合目标 APP/Web 完成生产基础设施适配与验收。

不要承诺“永远不会忘”“下载后无需配置即可承载海量用户”“所有推送绝对准时”“删除后所有备份瞬间物理消失”或“使用向量数据库就一定能找回”。
