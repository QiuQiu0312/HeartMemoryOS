# 给用户的一封信

你好，欢迎拿到“心忆 HeartMemoryOS”。

这不是只能观看的概念图，而是一套可以运行、测试和迁移的长期陪伴记忆架构。它附带了一个小型聊天页面，让没有现成 APP 或网站的人也能在自己的电脑上体验，甚至临时作为私人虚拟伴侣使用。

请先记住最重要的一点：

> 本地聊天页只是体验和测试记忆架构的外壳；能接入 APP、网页、机器人和服务器后端的记忆内核，才是这个项目的核心。

## 如果你只想马上试一试

先安装 Node.js 22.13 或更高版本，推荐当前 LTS。Node.js 可以理解成让这套程序在电脑上运行的基础环境。

- macOS：双击根目录的 `启动心忆.command`。
- Windows 10/11：双击根目录的 `启动心忆-Windows.bat`。

第一次启动会生成只供本机使用的配置，并安装界面依赖，可能需要几分钟。完成后浏览器会打开 `http://127.0.0.1:3000`。关闭启动窗口，会停止由本次启动的服务。

下载包只有不到几 MB 并不代表安装后仍然这么小：发布包故意不携带可自动重装、而且区分 Mac/Windows 的 `node_modules`。首次启动需要联网下载适合当前电脑的界面依赖；当前版本在 Mac 上安装后可能增加约 700～800 MB，建议至少预留 1 GB 磁盘空间。它们是运行网页界面需要的程序文件，不是你的聊天记录，也不消耗大模型 Token。

本地双击不会启动 Docker，也不要求你提前安装数据库。Windows 工具检测不到 Node.js 时会先征求同意，再尝试使用系统安装工具；无法自动安装时只会打开 Node.js 官方下载页。macOS 已实机验收，Windows 路径已做兼容与自动化检查，但尚未覆盖每一种 Windows 10 电脑。

## 第一次进入怎样使用

页面会先说明：这是本机测试页，不是已经完成公网账号、支付、手机推送和海量用户承载的商业 APP。

确认后：

1. 点右上角“设置”；
2. 在“模型 API”中填写主模型的 API 根地址与 API Key；
3. 点“读取模型”并选择平台支持的聊天模型；平台不支持列表时可以手填模型名；
4. 点“测试连通性”，确认成功后保存；
5. 回到聊天页开始对话。

只配置主模型就能聊天。后台整理模型默认复用主模型；想降低费用时，可以单独指定更便宜的小模型。Embedding 是可选能力，用来寻找“字不同但意思相近”的记忆；不配置时会自动使用人物名、关键词和全文检索。

项目内置了一位默认伴侣“心忆”。她温暖、俏皮，偶尔使用科技意象，日常回复较短。你可以在设置里修改名字、人设、相处方式、语气和第一句欢迎语；安全、隐私和权限边界不能被人设覆盖。

## 推荐完整测试一次

```text
正常聊几句
→ 说“请记住，我的猫叫小白”
→ 再聊几轮后追问猫叫什么
→ 明确纠正这条信息
→ 到记忆后台删除
→ 再次追问
```

聊天页右上角的“记忆后台”可以查看记忆、证据、授权、召回结果和版本化提示词。打开、刷新或浏览这些已有数据，不会调用模型。

## 哪些操作会花 Token

- 主模型生成回复：会产生 Token，是正常聊天的主要费用。
- 隐含记忆候选和滚动摘要：达到程序阈值后才调用后台模型。
- 明确说“请记住，具体内容”：程序能安全识别时直接保存，不增加一次记忆小模型调用。
- Embedding：只有单独配置、授权并开启后才调用。
- 打开后台、查看或修改人设、保存数据库、关键词检索、删除记忆：不调用模型。
- 第一条站内欢迎：使用固定模板，0 Token。
- “测试连通性”：会发出一次很小的真实请求，可能产生极少费用。

这里的“0 Token”只表示不调用模型，电脑或服务器仍会使用少量 CPU、内存、磁盘和网络。

API Key 只保存在本机服务端秘密文件里，页面与后台接口不会返回密钥原文。不要把 `.env`、`data/`、聊天数据库或 API Key 上传到公开仓库、发进群聊或打包给别人。

## 如果你已经有 APP、网站或机器人

通常不需要使用这个测试聊天页，也不用推倒原项目。正确方式是保留原来的前端、登录、聊天界面和管理后台，把心忆接在“主模型回复之前”和“回复成功以后”。

修改前请先备份原项目、数据库和服务器配置。然后把下面两样交给能够读取、修改并运行代码的编程大模型：

1. 完整的 HeartMemoryOS 项目，以及 `第一次打开后把这个文档发给大模型.MD`；
2. 你的完整成品或半成品项目目录。

部署手册会要求大模型先用生活化选项询问：电脑还是服务器、个人还是商用、单用户还是多用户、一个伴侣还是多个、是否已有后端与后台、旧聊天是否迁移。它应先只读检查和建立可恢复备份，再决定最小改动方案。

已有管理后台不会与心忆天然冲突。正式接入时通常复用原后台的登录、权限和设计，把这里的控制台当作参考页面或独立技术工具。不要同时维护两套互相矛盾的用户、授权或记忆真相。

## 如果你想直接放到服务器

个人服务器和公开商用不是同一种部署。只要服务暴露到互联网，就需要真实登录、HTTPS、备份、密钥管理和访问限制；多用户或商业产品还需要 PostgreSQL、严格用户隔离、独立后台任务、监控、容量和合规验收。

不要直接把本地测试配置上传公网，也不要因为目录里有 Docker 或 PostgreSQL 参考文件，就认为已经自动完成商业部署。具体使用宝塔、1Panel、Docker、PM2 或云平台，应由部署大模型读取真实服务器后再给步骤。

## 三份主要说明

- `README.md`：GitHub 首页与完整项目介绍；
- `第一次打开后把这个文档发给大模型.MD`：需求引导、接入和部署手册；
- `技术架构与部署说明.md`：架构、数据、安全、费用、迁移和生产验收细节。

## 开源许可

HeartMemoryOS 使用 MIT License。它允许个人和公司免费使用、修改、分发以及集成进商业或闭源产品，但分发代码或其实质部分时必须保留原版权声明与 MIT 许可文本。完整条款见根目录 `LICENSE`。

最后，用一句话说明心忆想解决的问题：

> 不是让 AI 把你说过的每个字永远塞在眼前，而是让它在正确的时候想起正确的事，同时让你保留查看、纠正、删除和关闭的权利。
